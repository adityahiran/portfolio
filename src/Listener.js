import React from 'react';

export class Listener extends React.Component {
  render() {
    return (
      <li>
        <span>
          Listener: A critical service that provides a reliable endpoint where the IoT devices can report data and receive acknowledgements. The raw data is then published and also persisted. [ Code Contribution: 80% ]
        </span>
      </li>
    );
  }
}
